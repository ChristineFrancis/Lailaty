import 'package:flutter/material.dart';

class FinalyPage extends StatelessWidget {
  const FinalyPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Center(
        child: Text('FINALLLLLYYYY!!!'),
      ),
    );
  }
}
